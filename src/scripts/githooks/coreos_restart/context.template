cluster:
  address: "{{{cluster.address}}}"
  port: "{{{cluster.port}}}"
  name: "{{{cluster.name}}}"
  id: "{{{cluster.id}}}"
app:
  name: "{{{app.name}}}"
  public_domain: "{{{app.public_domain}}}"
huxley:
  url: "{{{huxley.url}}}"
  token: "{{{huxley.token}}}"
